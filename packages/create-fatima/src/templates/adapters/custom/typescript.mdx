```ts
export default config({
  load: {
    development: [
      async () => {
        const fetch = async () => {
          return { NODE_ENV: "development" };
        };

        return await fetch();
      },
    ],
  },
});
```
