```ts
import { adapters } from "fatima";
import dotenv from "dotenv";

export default config({
  load: {
    development: [
      adapters.dotenv.load(dotenv),
      adapters.vercel.load(dotenv.parse),
    ],
  },
});
```
