```js
const { adapters } = require("fatima");
const dotenv = require("dotenv");
const trigger = require("@trigger.dev/sdk/v3");

module.exports = config({
  load: {
    development: [
      adapters.dotenv.load(dotenv),
      adapters.triggerDev.load(trigger),
    ],
  },
});
```
