```ts
import { adapters } from "fatima";
import dotenv from "dotenv";
import * as trigger from "@trigger.dev/sdk/v3";

export default config({
  load: {
    development: [adapters.dotenv.load(dotenv), adapters.trigger.load(trigger)],
  },
});
```
